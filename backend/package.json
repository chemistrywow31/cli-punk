{
  "name": "claude-punk-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "node -e \"const{platform}=require('os');if(platform()==='darwin'){require('child_process').execSync('chmod +x node_modules/node-pty/prebuilds/darwin-*/spawn-helper',{stdio:'ignore'}).toString()}\" || true"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  }
}
